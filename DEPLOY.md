# BROVA — ขึ้นระบบจริง

เอกสารนี้พาไปจากไฟล์ในเครื่อง จนถึงเว็บที่ทีมเปิดใช้งานได้จริง
ใช้เวลาประมาณหนึ่งชั่วโมง ไม่ต้องเขียนโค้ดเพิ่ม

สิ่งที่ต้องมี บัญชี GitHub บัญชี Supabase และบัญชี Vercel ทั้งสามอย่างสมัครฟรีได้

---

## ขั้นที่ 1 — เตรียมฐานข้อมูลที่ Supabase

สร้างโปรเจกต์ใหม่ที่ supabase.com เลือกภูมิภาค Singapore เพราะใกล้ไทยที่สุด
ตั้งรหัสผ่านฐานข้อมูลแล้วเก็บไว้ให้ดี รหัสนี้กู้คืนไม่ได้

เข้าเมนู SQL Editor แล้วรันไฟล์ในโฟลเดอร์ `supabase/` **ตามลำดับเลข** ทีละไฟล์
เปิดไฟล์ คัดลอกทั้งไฟล์ วางลงช่อง แล้วกด Run รอให้ขึ้นว่าสำเร็จก่อนค่อยทำไฟล์ถัดไป

| ไฟล์ | สร้างอะไร |
| --- | --- |
| `01_schema.sql` | ตารางหลักทั้งหมด ระบบเลขที่เอกสาร และฟังก์ชันหน้าติดตามงานของลูกค้า |
| `02_rls.sql` | กฎความปลอดภัยระดับแถว คนที่ไม่ได้ล็อกอินอ่านอะไรไม่ได้เลย |
| `03_seed.sql` | ข้อมูลตัวอย่าง ไว้ลองใช้ก่อนคีย์งานจริง |
| `04_accounting.sql` | ใบวางบิล ใบเสร็จ ใบลดหนี้ และการรับชำระเงิน |
| `05_editor.sql` | หน้าแก้ไขเอกสารคู่พรีวิว A4 |
| `06_purchasing.sql` | ใบสั่งซื้อ การรับของ และรอบนับสต็อก |
| `07_fabrics.sql` | คลังผ้าสิบชนิดพร้อมตารางราคาตามไซส์ |
| `08_approvals.sql` | ระบบขออนุมัติจากเจ้าของ |
| `10_line.sql` | ผู้ใช้ อีเมล และการแจ้งเตือนเข้าไลน์ |

**ห้ามรัน** `dev_auth_stub.sql` และ `dev_local.sql` บน Supabase
สองไฟล์นั้นมีไว้จำลองระบบล็อกอินตอนทดสอบบนเครื่องเท่านั้น
Supabase มีระบบล็อกอินของจริงอยู่แล้ว

ไฟล์ `11_go_live.sql` ยังไม่ต้องรันตอนนี้ เอาไว้รันตอนพร้อมเลิกใช้ข้อมูลตัวอย่าง

ตรวจว่าสำเร็จ ไปที่ Table Editor ต้องเห็นตารางประมาณสี่สิบตาราง
และตาราง `fabrics` ต้องมีสิบแถว

---

## ขั้นที่ 2 — เอาโค้ดขึ้น GitHub

```bash
cd brova-studio
git init
git add .
git commit -m "BROVA ระบบหลังบ้าน"
git branch -M main
git remote add origin https://github.com/ชื่อคุณ/brova-studio.git
git push -u origin main
```

ตั้งค่าที่เก็บโค้ดเป็น **Private** เพราะมีโครงสร้างธุรกิจและตารางราคาอยู่ข้างใน

ไฟล์ `.gitignore` กัน `.env.local` ไว้แล้ว กุญแจจึงไม่หลุดขึ้น GitHub
ก่อน push ลองสั่ง `git status` ดูให้แน่ใจว่าไม่มี `.env.local` อยู่ในรายการ

---

## ขั้นที่ 3 — เอาเว็บขึ้น Vercel

ที่ vercel.com กด New Project แล้วเลือกที่เก็บโค้ดที่เพิ่ง push ขึ้นไป
Vercel จะรู้เองว่าเป็น Next.js ไม่ต้องตั้งค่าอะไรเพิ่ม

ก่อนกด Deploy ใส่ค่าสองตัวในช่อง Environment Variables

| ชื่อ | เอาค่ามาจากไหน |
| --- | --- |
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase > Project Settings > API > Project URL |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | หน้าเดียวกัน ช่อง Project API keys บรรทัด `anon` `public` |
| `NEXT_PUBLIC_APP_URL` | ที่อยู่เว็บที่ Vercel จะให้ ใส่ทีหลังได้หลัง deploy รอบแรก |
| `SUPABASE_SERVICE_ROLE_KEY` | หน้าเดียวกัน บรรทัด `service_role` `secret` |
| `CRON_SECRET` | สุ่มข้อความเองยาวสัก 30 ตัว ใช้กันคนอื่นเรียกงานประจำวัน |

**เรื่องกุญแจ ต้องเข้าใจให้ตรงกัน**

ตัวแปรที่ขึ้นต้นด้วย `NEXT_PUBLIC` เบราว์เซอร์เห็นได้ทั้งหมด ซึ่งถูกต้องแล้วสำหรับสองตัวแรก
เพราะกุญแจ `anon` ถูกจำกัดด้วยกฎความปลอดภัยระดับแถวอยู่แล้ว

ส่วน `SUPABASE_SERVICE_ROLE_KEY` **ห้ามเติม `NEXT_PUBLIC` ข้างหน้าเด็ดขาด**
กุญแจนั้นข้ามกฎความปลอดภัยได้ทั้งหมด ถ้าเบราว์เซอร์เห็น ใครก็อ่านและแก้ข้อมูลลูกค้าทุกรายได้
ระบบใช้กุญแจนี้แค่สามงานที่ทำงานฝั่งเซิร์ฟเวอร์ล้วน ๆ คือสร้างผู้ใช้ รับข้อมูลจากไลน์
และงานสรุปประจำวัน ไม่มีทางถูกส่งไปที่หน้าเว็บ

กด Deploy รอประมาณสองนาที จะได้ที่อยู่เว็บมาหนึ่งอัน
แล้วกลับมาใส่ `NEXT_PUBLIC_APP_URL` ให้ตรงกับที่อยู่นั้น แล้ว deploy อีกครั้ง

---

## ขั้นที่ 4 — ให้ Supabase รู้จักที่อยู่เว็บ

กลับไปที่ Supabase > Authentication > URL Configuration

- **Site URL** ใส่ที่อยู่จาก Vercel เช่น `https://brova-studio.vercel.app`
- **Redirect URLs** เพิ่มที่อยู่เดียวกันต่อท้ายด้วย `/**`

ถ้าข้ามขั้นนี้ ล็อกอินแล้วจะถูกเด้งกลับหน้าเดิมวนไปเรื่อย ๆ

---

## ขั้นที่ 5 — ตั้งบัญชีเจ้าของและเพิ่มทีม

เปิดที่อยู่เว็บ ระบบจะรู้เองว่ายังไม่มีใครอยู่ในระบบ
แล้วเปลี่ยนหน้าเข้าสู่ระบบเป็นหน้า **ตั้งบัญชีเจ้าของ** ให้อัตโนมัติ

กรอกชื่อ อีเมล และรหัสผ่านที่จะใช้ แล้วกดสร้าง ระบบจะพาเข้าระบบให้ทันที
ไม่ต้องรอยืนยันอีเมล และไม่ต้องไปสร้างผู้ใช้ที่หลังบ้านของ Supabase

**บัญชีแรกจะได้สิทธิ์เจ้าของเสมอ และเป็นคนเดียวที่อนุมัติเรื่องนอกกรอบได้**

จากนั้นเพิ่มคนในทีมได้จากหน้า **ตั้งค่า** แล้ว **จัดการผู้ใช้**
กดเพิ่มผู้ใช้ ใส่ชื่อ อีเมล เลือกบทบาท ระบบสุ่มรหัสผ่านชั่วคราวให้เอง
แล้วแสดงอีเมลกับรหัสในกล่องเดียว กดคัดลอกไปส่งให้เจ้าตัวได้เลย

| บทบาท | เห็นและทำอะไรได้ |
| --- | --- |
| เจ้าของ | ทุกอย่าง อนุมัติได้คนเดียว ตั้งค่าระบบและผู้ใช้ |
| ฝ่ายขาย | เปิดงาน ทำใบเสนอราคา ดูแลลูกค้า |
| ฝ่ายออกแบบ | แบบร่างและรอบแก้แบบ |
| ฝ่ายผลิต | รับงานเข้าไลน์และบันทึกความคืบหน้า |
| ฝ่ายตรวจคุณภาพ | ตรวจและบันทึกผล |
| คลังสินค้า | รับของ จองผ้า นับสต็อก แพ็กของ |
| ฝ่ายบัญชี | วางบิล รับเงิน ออกใบเสร็จ เห็นต้นทุน |
| พาร์ตเนอร์ | เห็นเฉพาะงานของตัวเอง |

ลืมรหัสผ่านไม่ต้องตกใจ เจ้าของกดตั้งรหัสใหม่ให้ได้จากหน้าเดียวกัน
ระบบให้รหัสใหม่ทันที ไม่ต้องรออีเมล

ลบผู้ใช้ทิ้งไม่ได้ตั้งใจให้ทำ เพราะชื่อผูกอยู่กับประวัติงานและใบขออนุมัติ
ถ้าคนออกให้กดปิดใช้งานแทน ผลเหมือนกันคือเข้าระบบไม่ได้ แต่ประวัติยังอ่านรู้เรื่อง

---

## ขั้นที่ 6 — ปิดประตูหลัง

**สำคัญมาก ทำทันทีหลังเพิ่มทีมครบแล้ว**

ไปที่ Supabase > Authentication > Providers > Email แล้ว **ปิด Enable Sign Up**

ถ้าไม่ปิด ใครก็ตามที่รู้ที่อยู่เว็บจะสมัครเข้ามาเองได้
และคนที่สมัครเข้ามาจะได้สิทธิ์ฝ่ายขาย ซึ่งเห็นข้อมูลลูกค้าและยอดเงินทั้งหมด

---

## ขั้นที่ 7 — ใส่ข้อมูลบริษัท

ข้อมูลชุดนี้จะไปขึ้นบนหัวเอกสารทุกใบ ไปที่ Table Editor > `company_settings` แล้วแก้แถว `id = 1`

`name` ชื่อบริษัท · `tax_id` เลขประจำตัวผู้เสียภาษี · `address` ที่อยู่
`phone` `email` · `bank_name` `bank_account` `bank_account_name` สำหรับให้ลูกค้าโอน

เอกสารที่ออกไปแล้วจะไม่เปลี่ยนตาม เพราะเก็บข้อมูลบริษัทเป็นภาพนิ่งไว้ตอนออกใบ
แก้ตรงนี้จะมีผลกับเอกสารที่ออกหลังจากนี้เท่านั้น

---

## ขั้นที่ 8 — ลองใช้ให้ครบวง

ก่อนเลิกใช้ข้อมูลตัวอย่าง ลองเดินให้ครบหนึ่งรอบ

1. เปิดใบงานใหม่ กรอกโจทย์สามข้อให้ครบ
2. ลองย้ายสถานะ ดูว่าประตูตรวจกั้นตรงไหน
3. ลองยื่นขออนุมัติ แล้วเข้าไปกดอนุมัติในฐานะเจ้าของ
4. ออกใบวางบิล รับชำระเงินพร้อมแนบสลิป แล้วออกใบเสร็จ
5. เปิดหน้าติดตามงานที่ลูกค้าเห็น ตรวจว่าไม่มีต้นทุนหรือกำไรโผล่ออกไป
6. เปิดใบสั่งซื้อ รับของเข้าคลัง แล้วดูว่ายอดสต็อกขยับถูกต้อง
7. เปิดรอบนับสต็อก กรอกยอด แล้วปิดรอบ

ถ้าครบเจ็ดข้อนี้แล้วไม่ติดอะไร แปลว่าพร้อมใช้จริง

---

## ขั้นที่ 9 — ล้างข้อมูลตัวอย่าง

**สำรองฐานข้อมูลก่อน** ที่ Supabase > Database > Backups

เปิด `supabase/11_go_live.sql` หาบรรทัดนี้

```sql
-- set local brova.confirm_reset = 'ลบข้อมูลตัวอย่างทิ้ง';
```

ลบ `--` ข้างหน้าออก แล้วรันทั้งไฟล์ใน SQL Editor

ถ้าไม่ลบ `--` ไฟล์จะหยุดเองและไม่ลบอะไรเลย ตัวกันนี้ใส่ไว้เพราะการลบนี้ย้อนกลับไม่ได้

ไฟล์จะลบงาน เอกสาร การชำระเงิน ใบสั่งซื้อ รอบนับ ใบขออนุมัติ ลูกค้าตัวอย่าง
และโรงงานตัวอย่างสามเจ้า แล้วรีเซ็ตเลขที่เอกสารให้เริ่มที่ 0001 ใหม่

ที่**เก็บไว้** คือคลังผ้า ตารางราคา รหัสสินค้าทั้ง 81 รายการ ร้านคู่ค้าจริงสี่ร้าน
ข้อมูลบริษัท เพดานอนุมัติ และผู้ใช้ทุกคน

ยอดสต็อกจะถูกตั้งเป็นศูนย์ ให้ไปคีย์ยอดจริงผ่านหน้ารับของหรือหน้านับสต็อก
จะได้มีร่องรอยว่าของเข้ามาเมื่อไรและจากไหน

---

## ขั้นที่ 10 — ตั้งเพดานให้ตรงกับธุรกิจจริง

เข้าเว็บด้วยบัญชีเจ้าของ ไปหน้า **ขออนุมัติ** เลื่อนลงล่างสุด

| เพดาน | ค่าเริ่มต้น | ความหมาย |
| --- | --- | --- |
| ลดราคาได้เองไม่เกิน | 10% | ลดเกินนี้ต้องขออนุมัติก่อนออกเอกสาร |
| ให้เครดิตได้เองไม่เกิน | 30 วัน | ยาวกว่านี้ต้องขออนุมัติ |
| ใบสั่งซื้อไม่เกิน | 50,000 บาท | เกินนี้เปิดใบไม่ได้จนกว่าจะได้รับอนุมัติ |
| ส่งของก่อนชำระครบ | ต้องขออนุมัติ | ติ๊กเปิดได้ถ้าอยากให้ทีมทำเองได้ |
| ใบอนุมัติใช้ได้ | 72 ชั่วโมง | อนุมัติแล้วต้องไปทำรายการภายในเวลานี้ |

ปรับให้ตรงกับที่ทำจริง ตั้งไว้แน่นเกินไปทีมจะขออนุมัติทั้งวัน
ตั้งหลวมเกินไปก็ไม่ต่างจากไม่มีระบบอนุมัติ

---

## ขั้นที่ 11 — ตั้งค่าแจ้งเตือนเข้าไลน์

ข้ามได้ถ้ายังไม่ต้องการ ระบบทำงานครบโดยไม่มีการแจ้งเตือนก็ได้

**สิ่งที่ต้องรู้ก่อน** LINE Notify ที่หลายที่เคยใช้ ปิดบริการไปแล้วตั้งแต่ 31 มีนาคม 2568
ระบบนี้จึงใช้ LINE Official Account กับ Messaging API ซึ่งต้องสมัครบัญชีทางการ
แผนฟรีได้ 300 ข้อความต่อเดือน แผน Basic 1,280 บาทได้ 15,000 ข้อความ ราคายังไม่รวมภาษี

**การส่งเข้ากลุ่มนับเป็นหนึ่งข้อความ ไม่ได้นับตามจำนวนคนในกลุ่ม**
ถ้าออกแบบให้แจ้งเข้ากลุ่มทีมเป็นหลัก แผนฟรีมักพอสำหรับร้านเล็ก

ทำสี่ขั้นนี้

1. เข้า developers.line.biz สร้าง Provider แล้วสร้าง Channel แบบ Messaging API
   ระบบจะสร้างบัญชีทางการให้พร้อมคิวอาร์โค้ดของบอท
2. เข้าเว็บของคุณด้วยบัญชีเจ้าของ ไป **ตั้งค่า** แล้ว **แจ้งเตือนเข้าไลน์**
   วาง Channel access token แบบอายุยาว และ Channel secret ลงไป แล้วติ๊กเปิดใช้งาน
3. กลับไปที่หน้า Channel ช่อง Webhook URL ใส่ที่อยู่เว็บของคุณต่อท้ายด้วย
   `/api/line/webhook` แล้วเปิด Use webhook
   อย่าลืมเปิด Allow bot to join group chats ด้วย
4. เพิ่มบอทเป็นเพื่อน เชิญเข้ากลุ่มที่ต้องการ แล้วพิมพ์อะไรก็ได้ในกลุ่มหนึ่งครั้ง
   ระบบจะจับรหัสกลุ่มให้เองแล้วขึ้นในรายการปลายทาง กดเปิดใช้งานแล้วกดส่งทดสอบ

ถ้าข้ามขั้นที่ 3 ระบบจะจับรหัสกลุ่มให้ไม่ได้ ต้องไปหารหัสกลุ่มเองซึ่งยากมาก

**เลือกได้ว่าเรื่องไหนส่งเข้ากลุ่มไหน** เรื่องเงินเข้ากลุ่มบัญชี เรื่องผลิตเข้ากลุ่มโรงงาน
เรื่องรออนุมัติควรส่งเข้าไลน์ส่วนตัวของเจ้าของ ส่งทุกเรื่องเข้ากลุ่มเดียวคนจะเลิกอ่าน

**สรุปประจำวัน** ส่งอัตโนมัติทุกเช้า ตั้งเวลาได้ในหน้าเดียวกัน
ตัวจับเวลาอยู่ในไฟล์ `vercel.json` ตั้งไว้ที่ 08:30 ตามเวลาไทย
ถ้าจะเปลี่ยนเวลา แก้ในไฟล์นั้นด้วย จำไว้ว่าเลขในไฟล์เป็นเวลามาตรฐานสากล
ต้องลบเจ็ดชั่วโมงจากเวลาไทย

---

## เรื่องที่ควรรู้ไว้

**ลิงก์ติดตามงานของลูกค้าเปิดได้โดยไม่ต้องล็อกอิน**
แต่ละงานมีรหัสสุ่มของตัวเอง เดาลิงก์ของงานอื่นไม่ได้
หน้านั้นดึงข้อมูลผ่านฟังก์ชันที่คืนเฉพาะฟิลด์ปลอดภัยเท่านั้น
ต้นทุน กำไร ชื่อโรงงาน บันทึกภายใน และข้อมูลลูกค้ารายอื่น ไม่มีทางหลุดออกไป

**เอกสารที่ออกเลขแล้วแก้ไม่ได้**
เพราะเลขที่เอกสารต้องเรียงและห้ามซ้ำ ถ้าแก้ย้อนหลังได้
ใบที่ลูกค้าถืออยู่กับใบในระบบจะไม่ตรงกัน เวลาตรวจสอบจะพิสูจน์ไม่ได้ว่าอันไหนจริง
ถ้าออกผิดให้ยกเลิกแล้วออกใหม่ หรือออกใบลดหนี้

**ประตูตรวจสองบานข้ามไม่ได้ไม่ว่ากรณีใด**
G6 ตรวจคุณภาพก่อนแพ็ก และ G8 ต้องมีหลักฐานการส่ง
สองบานนี้เจ้าของก็ข้ามไม่ได้ เพราะเป็นเรื่องที่ผิดแล้วแก้ไม่ได้

**ใบอนุมัติใช้ได้ครั้งเดียว**
อนุมัติแล้วระบบจะตัดใบนั้นทิ้งทันทีที่เอาไปใช้ กันการหยิบไปใช้ซ้ำหรือใช้ข้ามงาน
และมีวันหมดอายุ กันการอนุมัติทิ้งไว้แล้วมีคนงัดมาใช้ทีหลัง

**ค่าใช้จ่ายรายเดือน**
แผนฟรีของ Supabase และ Vercel พอสำหรับทีมเล็ก
Supabase ฟรีจะพักฐานข้อมูลถ้าไม่มีใครใช้เกินหนึ่งสัปดาห์ ปลุกกลับมาได้ด้วยการกดปุ่มเดียว
ถ้าใช้ทุกวันก็ไม่เจอปัญหานี้ เมื่อข้อมูลเริ่มเยอะค่อยอัปเป็นแผนเสียเงินก็ยังทัน

---

## เวลามีปัญหา

**ล็อกอินแล้วเด้งกลับหน้าเดิมวนไปเรื่อย ๆ**
Site URL ที่ Supabase ยังไม่ตรงกับที่อยู่จริงบน Vercel กลับไปทำขั้นที่ 4

**สร้างผู้ใช้ไม่ได้ ขึ้นว่ายังไม่ได้ตั้งค่ากุญแจระบบ**
ยังไม่ได้ใส่ `SUPABASE_SERVICE_ROLE_KEY` ที่ Vercel หรือใส่แล้วแต่ยังไม่ได้ deploy ใหม่
ใส่แล้วต้องกด Redeploy หนึ่งครั้ง ตัวแปรถึงจะมีผล

**ส่งทดสอบเข้าไลน์ไม่ผ่าน**
สาเหตุที่พบบ่อยสามอย่าง โทเคนคัดลอกมาไม่ครบ บอทถูกเตะออกจากกลุ่มแล้ว
หรือแพ็กเกจไลน์ใช้ข้อความครบโควตาของเดือนนั้น
กดดูสาเหตุจริงได้ที่ประวัติการส่งท้ายหน้าตั้งค่าไลน์

**หน้าเว็บขึ้นว่าไม่มีข้อมูล ทั้งที่ใส่ไปแล้ว**
มักเป็นเพราะรันไฟล์ SQL ไม่ครบหรือรันสลับลำดับ
ตรวจที่ Table Editor ว่ามีตาราง `approvals` `fabrics` และ `line_settings` หรือยัง
ถ้าไม่มี ให้รันไฟล์นั้นใหม่ รันซ้ำได้ไม่เสียหาย

**กดอนุมัติแล้วขึ้นว่าเฉพาะเจ้าของเท่านั้นที่อนุมัติได้**
บัญชีที่ใช้อยู่ยังไม่ใช่ `owner` แก้ที่ตาราง `profiles` คอลัมน์ `role`

**เปิดใบสั่งซื้อไม่ได้ ขึ้นว่าเกินเพดาน**
ระบบทำงานถูกแล้ว ให้ยื่นขออนุมัติที่หน้าขออนุมัติ
หรือถ้าเพดานตั้งไว้ต่ำเกินจริง ให้เจ้าของไปปรับที่หน้าเดียวกัน

**อยากได้ข้อมูลตัวอย่างกลับมา**
รัน `03_seed.sql` ใหม่ได้ ไฟล์เขียนไว้ให้รันซ้ำแล้วไม่พัง
แต่ถ้าล้างด้วย `11_go_live.sql` ไปแล้วและคีย์งานจริงไปบ้างแล้ว อย่ารัน จะปนกัน
